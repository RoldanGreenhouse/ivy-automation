server {
    listen 80;
    server_name test.localhost;
    
    # Access control
    allow 192.168.0.0/16;
    allow 42.42.42.0/24;
    deny all;

    # Default location
    location / {
        root /usr/share/nginx/html;
        index /index.html;
    }
}